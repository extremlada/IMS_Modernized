<UserControl x:Class="IMS_Modernized.MVVM.View.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IMS_Modernized.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid Grid.Row="0" 
                  x:Name="dataGrid" 
                  d:ItemsSource="{d:SampleData ItemCount=5}" Margin="0,0,0,187" Grid.RowSpan="2" />
        <ComboBox Grid.Row="1" 
                  x:Name="comboBox" 
                  HorizontalAlignment="Center" 
                  Height="20"  
                  VerticalAlignment="Top" 
                  Width="128" Margin="0,35,0,0"/>
        <Button x:Name="ShowBTN" 
                Content="Show" 
                HorizontalAlignment="Center" 
                Height="32" 
                VerticalAlignment="Top" 
                Width="128" 
                Margin="0,82,0,0" 
                Grid.Row="1"/>
        <Grid.RowDefinitions>
            <RowDefinition Height="122*"/>
            <RowDefinition Height="103*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
    </Grid>
</UserControl>
